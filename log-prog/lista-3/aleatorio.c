#include <stdio.h>

int main(int argc, char **argv)
{
	int num;
	printf ("Digite um número inteiro para descobrir se ele é impar ou par: ");
	scanf ("%d", &num);
	resto = num % 2; 
	if (resto = 0)
		printf ("Par\n");
		if (num = 0)
	return 0;
}
